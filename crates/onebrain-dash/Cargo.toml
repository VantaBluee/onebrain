[package]
name = "onebrain-dash"
description = "Embedded dashboard SPA: static assets plus the router that serves them"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
axum = { workspace = true }
rust-embed = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
# Route tests follow the onebrain-api pattern: real server on an ephemeral
# loopback port, hit over real HTTP.
tokio = { workspace = true }
reqwest = { workspace = true }
