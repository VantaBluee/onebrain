[package]
name = "xtask"
description = "Repo automation: dist builds, smoke runs, cluster sim (cargo xtask ...)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
anyhow = { workspace = true }
# e2e embeddings step: decode encoding_format="base64" responses and read
# the smoke model's n_embd from its GGUF header (the dims oracle).
base64 = { workspace = true }
clap = { workspace = true }
onebrain-models = { workspace = true }
reqwest = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.10"
